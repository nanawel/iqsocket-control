monolog:
    handlers:
        console_log:
            type: console
            level: info

        file_handler:
            type: stream
            path: "%kernel.logs_dir%/everything.log"
            level: debug
